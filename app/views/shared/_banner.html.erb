<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path('jet.jpg') %>);">
  <div class="container">
    <h1>jetNgo allows <strong>people</strong> to <strong>fly anywhere</strong>!</h1>
    <p>Go anywhere you want.</p>
    <%= simple_form_for :search, url: root_path, method: :get do |f| %>
      <div class="search-form-control form-group">
        <%= form_tag movies_path, method: :get do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find a movie"
          %>
          <%= submit_tag "Search", class: "btn btn-gradient" style="padding: 0 8px;" %>
        <% end %>

<!--         <input class="form-control string required" type="text" name="search[query]" id="search_query" value="Search for a jet now"/>
        <button name="button" type="submit" class="btn btn-gradient" style="padding: 0 8px;">
          <i class="fas fa-search"></i> Search
        </button> -->
      </div>
    <% end %>
  </div>
</div>
